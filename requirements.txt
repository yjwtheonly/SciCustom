# Core
numpy>=1.24
pyyaml>=6.0
tqdm>=4.65

# Tagger inference - vLLM is preferred; transformers is the fallback.
vllm>=0.6.0; sys_platform != "darwin"
transformers>=4.44
torch>=2.1
sentencepiece>=0.1.99
accelerate>=0.30

# Fuzzy label matching (Section 3.3)
rapidfuzz>=3.6

# Subset selection (Section 2.5 / Appendix A)
scikit-learn>=1.3

# LLM clients used as judges and the MCQ writer
openai>=1.30
anthropic>=0.34
google-genai>=0.3
